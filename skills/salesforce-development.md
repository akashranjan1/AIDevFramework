# Salesforce development skill

Implement only the approved plan. Prefer bulk-safe Apex, explicit CRUD/FLS/sharing decisions, least-privilege permission changes, deterministic tests, and source-driven metadata. Do not use `SeeAllData=true` except where documented and unavoidable. Cover success, failure, bulk, security, and regression paths.

Return the JSON described in `schemas/agent-result.schema.json`, including changed files, tests, test output summary, risks, and rollback steps.
